# 架构:

1.聊天室的本质:  
很多客户端--通过tcp连接服务器--服务器-->处理用户请求--保存数据  
eg:Tom登录 Tom客户端--发送{msgid:LOGIN,username:Tom,password:123}--服务器--验证--Redis查询--返回成功  
这个过程存在4个不同的事情:  
1.消息怎么从客户端到服务器?              这是网络层面  
2.服务器接收到消息的格式                 这是协议问题  
3.对于登录,还有,群聊的处理               这是业务问题  
4.用户密码,好友关系,聊天记录等数据的保存   这是数据库问题  

因此,大块有:有服务器server,客户端client;网络库Netlib;协议protocol;业务Service(细分有UserService,FriendService,GroupService,MessageService,FileService);数据库database  

# 完整流程:


## 第一类:连接事件(客户端没有TCP连接)  
对象:  
listen_fd  
作用:  
接收新人  
流程:  
服务器启动(socket,bind...),listen_fd接收事件(客户端connect -> listen_fd)[EPOLLIN:有新的连接可以accept/有数据可以recv] 
想建立TCP连接,于是客户端连接服务器(connect)   
EventLoop在loop循环中调用了Epoller的wait函数,而Epoller中wait函数检测到listen_fd发生变化,返回变化个数,通知EventLoop  
EventLoop的for循环,将对应事件的listen_fd(这里还没有accept,故没有连接成功,故没有client_fd)和事件类型保存  
已知fd的EventLoop通过内部维护的fd→Channel映射表找到对应Channel,Channel根据不同的发生事件调用不同的处理函数Callback来接收客户端请求   
对于listen_fd,Callback调用TcpServer.accept(),接收client_fd  
而对于client_fd,Tcpserver创建TcpConnection,保存Jack的fd  

## 第二类：通信事件(已经建立连接)  
对象:  
client_fd  
作用：  
和已经连接的客户端通信  
流程:  
此时,不会再经过listen_fd,accept(),TcpConnection创建  
直接:  
client_fd产生EPOLLIN

↓

Epoller.wait()

↓

EventLoop

↓

client_fd对应Channel

↓

TcpConnection::handleRead()

↓

recv()

↓

业务处理


## 第一类：连接事件（listen_fd，新客户端建立 TCP 连接）
### 完整故事流程
饭店提前准备好：装好大门门铃（socket 创建 listen_fd）、挂上门牌地址（bind 绑定端口）、开启营业迎客（listen 开始监听）。  
路人（客户端）想要进店吃饭，走到门口按下门铃 connect()。  
大门门铃（listen_fd）触发响动 = EPOLLIN 事件。  
领班（epoll_wait）一直盯着所有设备，立刻发现：大门门铃响了！领班把消息上报前台 EventLoop。  
EventLoop 拿到消息：哪个设备响了？→ 大门门铃 listen_fd。  
找到门铃配套说明书（Channel），说明书规定：门铃响 = 接待新客人。执行门铃绑定的回调函数。  
回调函数执行 accept()：老板开门，把门外客人迎进来，分配一张独立餐桌 + 专属对讲器（生成 client_fd）。  
饭店新建一份【一桌客人服务档案 TcpConnection】，把这个桌号对讲器(client_fd) 存进去。  
### 👉 重点理解：
此时只有门铃 listen_fd 有事件，还不存在 client_fd！  
必须 accept 之后，才创建 client_fd，代表一条 TCP 连接正式建立。 
一个门铃，接待无数批客人，每批客人都分到全新独立对讲器。 
## 第二类：通信事件（client_fd，已经建立连接，数据收发）
### 前提：
客人早就进店落座，已经分配好餐桌和专属对讲器 (client_fd)。
### 完整故事流程
桌上客人想要点餐，按下自己桌的对讲器说话 → client_fd 产生 EPOLLIN。  
领班（epoll_wait）监测到：不是大门门铃响！是3 号桌对讲器有动静！上报前台 EventLoop。  
EventLoop 定位设备：3 号桌对讲器 client_fd。  
找到对讲器对应的 Channel（桌位操作说明书），执行对应回调 TcpConnection::handleRead()  
服务员拿起对讲器聆听 recv()，把客人说的话（网络数据）收下来。  
交给后厨处理（上层业务逻辑）。  
### 👉 关键区别：
客人已经进店，全程再也不会碰大门门铃 listen_fd，不会再调用 accept！ 
客人和饭店沟通，只用自己桌上专属对讲器 client_fd。 


# 1：同样都是 EPOLLIN，怎么区分是新连接还是客户端发数据？  
类比：领班看到设备有动静（EPOLLIN），先看是哪个设备发出的信号  
如果是大门门铃 listen_fd：新客人上门，执行 accept 接待    
如果是餐桌对讲器 client_fd：老客人说话，执行读数据 recv  

# 2：为什么通信阶段不再走 listen_fd？  
门铃只负责「招揽外面想进店的人」。  
人一旦进门落座，后续聊天点餐，自然不需要反复去按大门门铃。 

# 3：Channel 到底是什么？
每个 fd（门铃 / 对讲器）都绑定一张纸条 Channel：  
纸条内容：  
如果这个设备响了（EPOLLIN），执行 XX 函数；  
如果设备断线（EPOLLHUP），执行关闭清理函数。  
epoll 只负责通知哪个 fd 有动静，具体干什么活，全由 Channel 保存的回调决定。  

# 一个请求在聊天室里的完整生命周期
1. 举例:好友请求   
client  
 |  
 |  用户选择菜单  
 |  (输入6 添加好友)  
 ↓  
client.cc  
 |  
 | 构造json  
 |  
 | {  
 |   "msgid":6,  
 |   "from":"tom",  
 |   "to":"jack"  
 | }  
 |  
 | MessageCodec::encode()  
 |  
 | send(fd)  
 ↓  
server TcpConnection  
 |  
 | recv()  
 |  
 | 解码  
 |  
 | json::parse()  
 ↓  
MessageDispatcher  
 |  
 | 根据msgid判断  
 |  
 | case SEND_FRIEND_REQUEST_MSG:   
 |          
 |     FriendRequestService::sendRequest()          
 ↓      
Service层       
 |       
 | 参数检查        
 | 业务判断       
 |            
 | 调用Model         
 ↓        
Model层          
 |       
 | MySQL       
 |      
 | INSERT friend_request         
 ↓     
Service层       
 |        
 | 返回结果      
 |        
 | response json       
 ↓      
TcpConnection::send()    
 ↓         
client recv       
 ↓       
ClientMessageHandler        
 ↓   
显示:   
发送好友申请成功    

2. 文件传输   
第一阶段：   

文件请求：  

clientA   
 |  
 FILE_REQUEST_MSG     
 |     
server      
 |    
FileService    
 |   
保存文件信息    
 |       
send ACK   
 |    
send NOTIFY 
 |    
clientB


文件发送:  
clientA   
 |         
 FILE_DATA_MSG    
 |     
 server       
 |    
 保存/转发   
 |    
 clientB         
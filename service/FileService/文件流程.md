1客户端在client.cc输入需求:请求发送文件->整理json语句,send()   
2service的messagedispather recv()后,通过msgid调用不同service处理->  
3fileservice接收json语句->sendfile函数开始处理:  
3.1将发送文件请求存入mysql(通过创建json发给filemodel)  
3.2判断接收方(acceptor/toname)在线否(isonline)->  
3.2.1接收方在线:创建json语句直接推送  
3.2.2离线:redismanager保存文件相关信息,上线了推送  

4接收方接收文件:在client.cc输入,json语句send()->  
5service的messagedispather recv()后,通过msgid调用不同service处理->  
6fileservice接收json语句->acceptfile函数开始处理:  
7.1修改数据库status从0到1  
7.2通知sender(),可以发送了  

8sender()的fileclient.cc读取文件,切块,encodeBinary()生成:totalLen+msgid+jsonLen+json+binary data ,通过SocketUtil::sendAll()发送出去->  
9服务器通过receiveFileData()收到->转发给acceptor端->  
10recvmessage()判断if(msgid==FILE_DATA_MSG),进入receiveFile()->写入data/files/recv/文件名  
11FILE_FINISH_MSG->server,status=2

# 流程:
正常: 
send block 0

↓

server

↓

receiver pwrite

↓

receiver ACK

↓

ClientMessageHandler

↓

notifyBlockAck(64,0)

↓

receivedAcks_插入:

{64,0}

↓

waitForBlockAck唤醒

↓

发现:

receivedAcks_.count({64,0})==1

↓

发送block1

接收方掉线:
send block100

↓

server

↓

getConnection失败

↓

return

↓

没有ACK

↓

waitForBlockAck等待5秒

↓

false

↓

sendFile return

↓

停止发送
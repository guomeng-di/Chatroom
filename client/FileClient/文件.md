发送方：

FileClient::sendFile()

↓

服务器：

TcpConnection::handleRead()

↓

进入：

FileService::receiveFileData(packet,this);





发送方 FileClient::sendFile()
        |
        | FILE_DATA_MSG（二进制）
        v
服务器
        |
        | 目前不处理 FILE_DATA_MSG
        v
接收方 FileClient::receiveFile()
        |
        | 写入磁盘
        v
FILE_BLOCK_ACK
        |
        v
服务器转发 ACK
        |
        v
发送方 waitForBlockAck()
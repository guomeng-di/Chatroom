普通成员函数（无 static）
教室模板（类），想用必须单独申请一间教室（RedisManager r; 造对象），每个教室独立，有自己的桌椅、网线（成员变量，比如 redis 连接）。
不同教室互不干扰。
static 静态函数
没有实体教室！
只是一块露天公共黑板，不属于任何一间教室，大家直接过来写字。
👉 关键点：它没有配套的桌椅、网线（不能访问普通成员变量）
它只是一个独立的工具，不绑定任何实体房间。
单例
✅ 确实全局从头到尾，只允许存在【一间实体教室】，所有人共用这一间。
这间教室完完整整，有自己的桌椅、网线（可以正常使用普通成员变量）
所有人想要使用，都只能来这唯一一间。
